module.exports = async () => {
    let {
        Op,
        Sequelize,
        Student,
        Teacher,
        Homework,
        Source_download_history,
        Visit_history,
        Subject,
        Homework_submit_history,
        Login_history,
        University,
    } = require('./index');
    console.log('seeding');
    console.log('seeding end');
}